<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kelvin.uasandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UasAndroid">
        <activity android:name=".InsertActivity"></activity>
        <activity android:name=".EditActivity" />
        <activity android:name=".model.PutUser" />
        <activity android:name=".model.UpdateUser" />
        <activity android:name=".model.DeleteUser" />
        <activity android:name=".services.ApiClient" />
        <activity android:name=".model.User" />
        <activity android:name=".model.GetUser" />
        <activity android:name=".User" />
        <activity android:name=".Getuser" />
        <activity android:name=".UsersAdapter" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>